{% extends 'default.html.twig' %}

{% block main %}
    <h1>Home Page</h1>
    <p class="lead">Hi, {{ name }}</p>
    <p class="lead">
        <a href="#" class="btn btn-lg btn-light fw-bold border-white bg-white">Больше текста</a>
    </p>
{% endblock %}